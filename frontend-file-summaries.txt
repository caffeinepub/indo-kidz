{
  "frontend/src/index.css": "Global styles with OKLCH warm/energetic color palette (yellows, oranges, greens), imports Fredoka One and Nunito Google Fonts, and sets up the INDO KIDZ playful design system with custom CSS variables for both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the theme with Fredoka One and Nunito font families, warm OKLCH color tokens, playful box shadows, extra border-radius values, and custom bounce/wiggle animations.",
  "frontend/src/App.tsx": "Main app entry point using TanStack Router with routes for Homepage (/), Fees Payment (/fees), and Admin Panel (/admin). Includes a Layout component with Navbar, Footer, ProfileSetupModal (for first-time users), and Sonner Toaster.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the backend; fixes the TypeScript error by importing and using the `HomepageContent` type directly instead of inferring it via `Parameters<typeof actor.updateHomepageContent>`, and retains all prior fixes including `isPending`-based owner loading state and admin role assignment after owner registration.",
  "frontend/src/components/Navbar.tsx": "Sticky navigation bar that now uses `useHasOwner` to gate the owner registration flow — only calls `registerOwner` when `hasOwner` is confirmed false, preventing premature or duplicate registration attempts, and shows the \"Customize Site\" button only after `ownerFetched` is true and `isOwner` is confirmed.",
  "frontend/src/components/Footer.tsx": "Playful footer with school branding, quick links (including Contact Us scroll link), location info, copyright year, and caffeine.ai attribution — the static contact info (email/phone) block has been removed.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog that appears for first-time authenticated users to enter their name, saved via saveCallerUserProfile() with success/error toast feedback.",
  "frontend/src/components/AccessDeniedScreen.tsx": "Friendly access-denied screen shown when a non-admin user tries to access the admin panel, with a link back to the homepage.",
  "frontend/src/pages/Homepage.tsx": "Public homepage with Hero, About Us, Banner, Visit Us (location), Contact Us (displays contactInfo from backend), Fees Preview, and CTA sections — the Learning Methods and Student Connection sections have been removed.",
  "frontend/src/pages/FeesPayment.tsx": "Fees payment page showing available fee categories as selectable cards and a form to submit a payment record (student name, fee type, amount). Displays a success confirmation with record ID after submission.",
  "frontend/src/pages/AdminPanel.tsx": "Owner-only admin panel with tabs for Content, Fee Categories, and Payment Records; uses the corrected `useIsOwner` hook (with `isPending`-based loading) to reliably block non-owners and show a loading spinner until the ownership check fully resolves.",
  "frontend/src/components/admin/ContentEditor.tsx": "Admin content editor with five editable fields (heroText, tagline, aboutUs, schoolAddress, contactInfo) — learningMethods and studentConnection fields removed, contactInfo field added with label, tooltip, and placeholder so the owner can edit the Contact Us section.",
  "frontend/src/components/admin/FeeCategoryManager.tsx": "Admin component to add, inline-edit, and delete fee categories with confirmation dialogs for destructive actions and toast notifications.",
  "frontend/src/components/admin/PaymentRecordsTable.tsx": "Admin component displaying all payment records with search/filter controls, status badges, and inline status update dropdowns with loading indicators."
}